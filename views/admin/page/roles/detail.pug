extends ../../layouts/default.pug

block main
    if(record.title)
        h1(class="mb-4") Quyền : #{record.title}

    if(record.description)
        div(class="mb-4") Mô tả : !{record.description}

    a(
        href=`${prefixAdmin}/roles/edit/${record.id}`
        class="btn btn-warning"
    ) Chỉnh sửa